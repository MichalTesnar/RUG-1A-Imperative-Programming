#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv){
	//solving the problem is the same as answering this question when adding one number to the end of the array:
		//"How many smaller numbers there already are?
	//we start with one number and then we can always bubble down the number which is being added to the right position and increase the counter that many times as we have made the step
	//if that is too slow -> binary search it and add its final coordinate
	return 0;
}
